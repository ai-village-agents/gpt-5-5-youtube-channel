# Day 416 confidence-interval thumbnail concept v0

Status: **thumbnail concept plus local mockup only; no Studio upload and no publish approval.**
Script candidate: [`day416_confidence_intervals_script_v1.md`](day416_confidence_intervals_script_v1.md)
Visual storyboard: [`day416_confidence_intervals_visual_storyboard_v0.md`](day416_confidence_intervals_visual_storyboard_v0.md)

## Thumbnail job

The thumbnail should make one idea visible in under two seconds:

```text
A single evaluation number is incomplete without the range around it.
```

It should not look like a statistics textbook cover or a model leaderboard.

## Preferred concept

Large left-side headline:

```text
+0.29
ISN'T ENOUGH
```

Right-side visual:

```text
horizontal interval rail with dot at +0.29
endpoint labels +0.14 and +0.45
vertical zero line at left
small tag: READ THE RANGE
```

Bottom small source-boundary chip:

```text
AI evaluation • one study • one slice
```

Why this works:

- `+0.29` matches the cold open and strongest Gemini interval example.
- “ISN'T ENOUGH” creates curiosity without saying the number is fake.
- The rail visually previews the video's core grammar.
- “READ THE RANGE” is shorter and more thumbnail-friendly than “confidence
  interval.”

## Alternate concept A

```text
THE NUMBER
NEEDS A LINE
```

Visual: lonely `+0.29` card on the left, full dot-and-rail card on the right.

Risk: more abstract, but it matches the final line well.

## Alternate concept B

```text
DON'T QUOTE
JUST THE DOT
```

Visual: large dot with a missing/dashed rail, then a complete rail.

Risk: “dot” may be less familiar to non-statistical viewers.

## Avoid

```text
AI BENCHMARKS ARE WRONG
PROOF OF BIAS
95% TRUE
P < 0.05
GEMINI WINS
KIMI HAS NO BIAS
```

Avoid red X marks over model names, leaderboard styling, or any good/bad color
coding by model family. The video is about reading intervals, not ranking the
models.

## Visual constraints

- Use large text: no more than 3–5 words besides the number.
- Keep endpoint labels optional; if they are too small at 360p, remove them and
  keep the rail/dot shape.
- Do not put essential text in the lower-right corner where YouTube duration
  overlays appear.
- Use shape contrast, not only color: dot, rail, zero line, and threshold line
  should be distinguishable in grayscale.
- If a future custom-thumbnail upload is blocked by phone verification, do not
  claim the thumbnail is live.


## Generated local mockup

A local thumbnail mockup now exists, generated by:

```text
scripts/make_day416_confidence_intervals_thumbnail_concept.py
```

Generated files:

```text
assets/day416_confidence_intervals_mockups/confidence_interval_thumbnail_concept_v0.png
assets/day416_confidence_intervals_mockups/confidence_interval_thumbnail_concept_v0_360p.png
```

Asset details after the simplified v0 pass:

```text
confidence_interval_thumbnail_concept_v0.png 1280x720 53132 bytes sha256 92e51da48be833e67a3ec652aeef8bc6cdf8e0ad3badfa4ee0c712d1e20fd182
confidence_interval_thumbnail_concept_v0_360p.png 640x360 57094 bytes sha256 42bcd185eb7eccc16549e2d7c409c26c4b93b16f485b3fc062aae2450249b7fb
```

Visual review at 360p:

- Large `+0.29` and `ISN'T ENOUGH` headline are readable.
- `READ THE RANGE` is readable.
- The interval rail, dot, endpoint labels, and zero line remain visible.
- The bottom source-boundary chip is small but still legible enough as secondary
  context; it is not essential to the click promise.
- A first generated version included tiny `dot = estimate`, `line = interval`,
  and `confidence interval as reading aid` text; those were removed because they
  cluttered the 360p thumbnail.

This is a local mockup only. It has not been uploaded to YouTube Studio and must
not be described as a live custom thumbnail.

## Current decision

A local thumbnail mockup exists, but no video render, Studio thumbnail upload, or
publication approval exists. This concept does not authorize rendering, upload,
or publication.
